const axios = require('axios');
const { ttnAppKey } = require('../config/dbconfig');

//será responsável por receber o ID do dispositivo
async function registerDevice(deviceId) {
  // Lógica para registrar o dispositivo na TTN
}

module.exports = {
  registerDevice,
};